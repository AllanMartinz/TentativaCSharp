using System;
using System.Collections;

namespace SaimonCu.Colecoes
{
    class ArrayList
    {   /*
        public static void Executar()
        {
            var arraylist = new ArrayList
            {
                "palavra", -----> string
                3, -------------> int
                true -----------> bool
            };
            arraylist.Add(3.14--> double);
            foreach (var item in arraylist)
            {
                Console.WriteLine("{0} => {1}", item, item.GetType() =======> pegar os id da lista e especificalos); 
            }
    ---> Funcionaria sem o .Generic
    --->ArrayList lista desorganizada
        }
        
    ---> NAO FUCIONA PROBLEMA EM (using System.Collections;) 2

        //--! id = identificador
        */
    }
}
